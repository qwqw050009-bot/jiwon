# 지원사업 마감판

정부·지자체 지원사업 공고를 마감일 순으로 보여주는 정적 사이트.

## 구조
- 데이터: 기업마당 오픈API (`src/bizinfo.py`)
- 빌드: Python + Jinja2 → `dist/` 정적 생성 (`src/build.py`)
- 배포: GitHub Actions → Cloudflare Pages (매일 06:00 KST + push 시)
- 사이트: https://jiwon-5i5.pages.dev

## 파일 역할
- `src/config.py` — 사이트 설정, 분야 8종, 지역 17종
- `src/bizinfo.py` — API 어댑터. 응답을 내부 스키마로 정규화
- `src/sources.py` — 데이터 로더. D-day 계산, 정렬
- `src/enrich.py` — 공고별 해설 생성. 캐시 필수
- `src/build.py` — 전체 페이지 생성, sitemap, robots
- `src/ics.py` — 캘린더 구독 파일
- `src/pages.py` — 애드센스 필수 고정 페이지
- `templates/` — Jinja2 템플릿
- `static/filter.js` — 지역·분야 클라이언트 필터
- `static/scrap.js` — 로그인 없는 스크랩 (localStorage)

## 반드시 지킬 것

**1. 지역 파싱을 추측하지 마라**
지역은 `_region()`에서 제목 접두 `[경기]`, `jrsdInsttNm`, 해시태그
세 곳을 교차 확인한다. 정규식으로 임의 추측하는 코드를 넣지 마라.
못 찾으면 "전국"으로 둔다.
`전남광주통합특별시`는 광주+전남 통합 단위다. 분리하지 마라.

**2. 실데이터에 없는 필드를 가정하지 마라**
API에 지원금액 필드가 없다. 본문에서 추출한다(`enrich.amount_of`).
모든 필드 접근은 `.get()` 으로 방어한다.
목업 스키마와 실데이터 스키마가 다르면 실데이터 기준으로 맞춘다.

**3. 상시 접수 공고를 버리지 마라**
전체의 약 18%가 `예산 소진시까지`, `상시 접수` 등 날짜가 아니다.
`period_type`이 `always`면 목록 하단에 배치하고 원문 표기를 그대로 노출한다.
마감일이 없으므로 캘린더(.ics)에는 넣지 않는다.

**4. 정적 페이지를 없애지 마라**
SEO의 본체는 서버에서 미리 생성한 지역×분야 페이지다.
JS 필터는 그 위에 얹는 레이어일 뿐이다.
JS가 꺼져도 서버 렌더 목록이 보여야 한다.

**5. LLM 호출은 반드시 캐시를 거쳐라**
`enrich.py`의 캐시를 우회하는 코드를 넣지 마라.
공고 하나당 평생 1회만 호출한다.

**6. 색인 설정을 함부로 켜지 마라**
`config.py`의 `allow_index`는 도메인 연결 전까지 False다.
무료 주소로 색인되면 도메인 이전 시 중복 콘텐츠가 된다.

**7. 인증키를 코드에 쓰지 마라**
`BIZINFO_KEY`, `ANTHROPIC_API_KEY`는 환경변수로만 읽는다.

## 작업 방식
- 수정 후 반드시 `python src/build.py` 로 빌드가 통과하는지 확인
- 페이지 수가 갑자기 줄면 뭔가 깨진 것이니 원인을 찾고 보고할 것
- 확인 끝나면 커밋하고 push (push하면 자동 배포됨)
- 커밋 메시지는 한국어로 무엇을 왜 고쳤는지 한 줄
